package com.github.glo2003.handlers;

import org.junit.Assert;
import org.junit.Test;
import spark.Request;

public class TestScheduleHandler {

    /**
     * Test bad param send to schedule handler
     */
    @Test
    public void testParamScheduleHandler()
    {
        ScheduleHandler handler = new ScheduleHandler();

        //handler.handle();
        Assert.assertTrue(true);
    }

}
